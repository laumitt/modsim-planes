class Airport(self, code, size, destinations):
    self.code = code

class Plane(self, number, breakage, loc, dest, eta):
    self.number = number

def check_terminal():
    if terminal_free == True:
        plane_terminal = next_terminal
    else:
        plane_terminal = terminal_0
    
